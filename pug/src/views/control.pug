html
    include includes/head.pug
    body
        include includes/nav.pug
        div
            h2(class="text-center m-3")="PANEL DE CONTROL"
                div 
                    form(action="/productos", method="post", class="panel")
                        h2(class="mb-4 text-center")="Agregar un producto"
                        div(class="mb-3")
                            label(class="form-label")="Titulo"
                            input(type="text", name="title" class="form-control", placeholder="No title")
                        div(class="mb-3")
                            label(class="form-label")="Precio"
                            input(type="number", name="price" class="form-control", placeholder="0")
                        div(class="mb-3")
                            label(class="form-label")="Imagen"
                            input(type="text", name="thumbnail" class="form-control", placeholder="Image URL")
                        button(type="submit", class="btn btn-success mt-3", style="width: 40%; align-self: center;")="Añadir producto"